<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
</head>
<body>
   <div class="main w-full min-h-screen bg-zinc-900">
    <div class ="form p-10 text-white">
        <form action=""> 
            <input class="block w-full rounded-md px-5 py-2 bg-zinc-800" type="text" placeholder="Title Goes Here" name="title">
            <textarea class="block w-full rounded-md mt-2  px-5 py-2 resize-none bg-zinc-800" placeholder="Write Your Notes Here..." name="details"></textarea>
            <input class="mt-2 bg-blue-600 px-5 py-2 rounded-md" type="submit" value="Create Notes">
        </form>
    </div>

    <div class="tasks p-10 flex gap-3 flex-wrap" >
    <% if(files.length > 0){ %>
    <% files.forEach(function(val){ %> 
         
            <div class="task w-72 hpx-3 py-4 rounded-md bg-zinc-800 ">
                <h1 class="text-white text-3xl tracking-tighter"> Some Notes Here </h1>
                <a class="text-blue-500 inline-block mt-3" href="">Read More</a>
            </div>

            <% }) %>
        <% }  else { %>
            <h3 class="text-zinc-600">No Notes Yet</h3> 
    <% } %>
   </div>
</body>
</html>
